package com.fftw.util.settlement.calendar;

import com.fftw.db.BaseUnitTest;

public class TestBusinessCalendar extends BaseUnitTest
{

    //private MarketCalendar mc;
/*    
    @Override
    @BeforeClass(groups = {"unittest"})
    public void setUp() throws DatabaseUnitException, SQLException, Exception {
//        super.setUp();
//        mc = new MarketCalendar();
        
    }
    
    @Test(groups =
    {
        "unittest"
    })
    public void testSettlementDate ()
    {

        MarketCalendar bc = MarketCalendar.getInstance("NYSE");

        LocalDate id1 = new LocalDate(2007, 8, 10);
        LocalDate sd1 = bc.determineDate(id1, SettlementDate.T3);
        assertEquals(new LocalDate(2007, 8, 15), sd1);

        LocalDate sd2 = bc.moveSettlementDate(new LocalDate(2007, 8, 15), -3);
        assertEquals(new LocalDate(2007, 8, 10), sd2);

        LocalDate sd3 = bc.moveSettlementDate(new LocalDate(2007, 8, 15), 0);
        assertEquals(new LocalDate(2007, 8, 15), sd3);
S
    }
*/
}
